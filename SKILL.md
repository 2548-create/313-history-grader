---
name: essay-grader
description: >
  313历史学统考论述题自动批改与反馈生成。当用户上传图片（学生答案）时触发；
  用户直接粘贴题目+答案文本时也触发。自动完成OCR识别、独立生成参考答案、踩分点拆解、
  逐点评分、问题诊断，最终输出Word学生反馈报告。不适用于：名词解释、简答题、史料分析题、选择题批改。
---

# 313历史学统考论述题批改技能

## 核心定位

本技能将历史学论述题批改从"凭感觉打分"变成"有据可查的结构化评估"。批改结果不是冷冰冰的数字，而是一份**学生看得进去、改得动笔、复习有方向**的反馈文件。

**重要原则**：
- 评分理由必须严格对照学生答案实际内容，禁止模板化套用
- 得分预估必须与总分概览表的合计值完全一致，禁止手写不同数字

---

## 工作流总览

```
┌──────────────────────────────────────────────────────────────┐
│                    第一步：输入确认                           │
│              题目文本 / 学生答案图片                           │
└──────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────┐
│              阶段一：独立生成（严禁查看学生答案）               │
├──────────────────────────────────────────────────────────────┤
│  第二步：生成参考答案                                          │
│    → 脚本：generate_answer()                                 │
│    → 自检：第二步自检 (D项)                                   │
│  第三步：提取踩分点                                            │
│    → 脚本：extract_scoring_points()                          │
│    → 自检：第三步自检                                         │
└──────────────────────────────────────────────────────────────┘
                            ↓
            【阶段切换承诺】输出承诺书
                            ↓
┌──────────────────────────────────────────────────────────────┐
│              阶段二：对照评分（可查看学生答案）                 │
├──────────────────────────────────────────────────────────────┤
│  第四步：OCR识别学生答案                                       │
│    → 脚本：ocr_student_answer() + red_marker()               │
│    → 自检：第四步自检 (A项部分)                                │
│  第五步：逐点评分                                              │
│    → 脚本：score_points()                                    │
│    → 自检：第五步自检 (A项完整 + E项)                          │
│  第六步：论述组织评分                                           │
│    → 脚本：score_language()                                  │
│    → 自检：第六步自检                                         │
│  第七步：本题诊断                                              │
│    → 脚本：generate_diagnosis()                              │
│    → 自检：第七步自检 (B项完整)                                │
└──────────────────────────────────────────────────────────────┘
                            ↓
              全局自检（A-F）→ grade_checker.py → 输出Word
```

**两阶段隔离规则**：
- **阶段一**（第二步、第三步）：❌ 严禁查看任何学生答案图片
- **阶段二**（第四步至第七步）：✅ 可以查看学生答案
- **违反后果**：参考答案被学生答案带偏 → 踩分点不全 → 评分失准 → 反馈无效

---

## AI用语规范（全局适用）

### 一、禁用词汇总

| 类别 | 禁用词/表述 | 问题类型 |
|------|------------|---------|
| **现代社科腔** | 受…驱动、遵循…的逻辑、核心矛盾在于、结构性变化、双重驱动、根本转变、历史必然性、底层逻辑、结构性弱点 | 抽象化、现代化 |
| **现代经济术语** | 规模效应、制度效率、增长模式、不可持续性、收益递减、执行成本、治理成本、资源配置 | 经济学术语 |
| **过度修饰** | 深刻地、极大地、显著地、前所未有地、至关重要 | 形容词堆砌 |
| **绝对化表述** | 完全、彻底、永远、所有、一切（注意：历史表述中有时合理，需结合上下文判断） | 缺乏历史分寸感 |
| **异常内容** | 英文残留、乱码、异常字符 | 格式错误 |

### 二、正确表述准则

| 问题 | 错误示例 | 正确示例 |
|------|---------|---------|
| **抽象主语** | "制度效率的提升推动了经济发展" | "朱元璋推行屯田制，使军队自给自足，减轻了百姓赋税负担" |
| **抽象因果** | "受利益驱动，商人寻求新航路" | "香料利润丰厚，西欧商人利润受损，故寻求新航路" |
| **空泛概括** | "该措施具有深远影响" | "粮长制沿用至明中后期，影响了明朝财政管理" |
| **过度修饰** | "深刻地改变了历史进程" | "奠定了明朝近三百年的统治基础" |
| **绝对化** | "完全废除了封建制度" | "进一步加强了中央集权" |

### 三、自查清单（写完必查）

1. **主语检查**：每句话主语是具体历史主体（人/群体/政权），而非抽象名词？
2. **因果检查**：因果表述是否用"随着""由于""这导致""由此"等历史学惯用表达？
3. **修饰检查**：是否删除了"深刻地""极大地""显著地"等过度修饰？
4. **绝对化检查**：是否删除了"完全""彻底""永远"等绝对化表述？
5. **英文残留检查**：是否包含英文单词或字母？
6. **论文检验**：这段话能否直接出现在历史学论文中？

---

## 第一步：输入确认

**【输入要求】**
- 题目文本（用户直接粘贴）或学生答案图片

**【处理规则】**
- 情况A：用户直接提供题目文本 → 直接使用，进入第二步
- 情况B：用户只上传图片 → 快速识别图片顶部题目区域（不读学生答案），识别成功则使用，失败则问用户

**【输出格式】**
- 确认题目文本，进入下一步

**【检查点】**
- [ ] 题目文本已确认
- [ ] 若只有图片，已识别题目区域且未读学生答案

**【脚本实现】** 无（人工确认）
**【自检器检查】** 无

**【常见错误】** 无

**【违规案例】** 无

**【批量模式说明】** 若用户一次提供多道题目，记录题目列表，后续步骤逐题执行。

---

## 第二步：判断题目类型与生成参考答案（阶段一·独立生成）

**【输入要求】**
- 题目文本

**【处理规则】**
1. 判断题目类型（措施类/原因类/影响类/状况类/比较类/评述类）
2. 先看题目设问，按题目逻辑组织答案结构，题目本身答案优先于任何维度模板
3. 需要时查阅 `references/维度参考.md`（**用相对路径**，如 `references/维度参考.md`，以本 skill 目录为基准；禁止写死 `C:/Users/妄/...` 这类绝对路径，换机器/换用户即失效）
4. **确定板块（板块由题目子问项决定，非参考答案生成）**：从题目设问直接拆解出的子问项即为板块。例："演变及影响"→[演变,影响]；"措施及影响"→[措施,影响]；"背景、过程、影响"→[背景,过程,影响]。**板块数 = 题目子问项数**，不得为保分数而拆分或合并板块。
5. 完成分值规划：各板块满分之和=30，**单板块≤18**，按踩分点数量比例分配。该板块结构**同时决定** L2 踩分表 sections 与 L1 参考答案顶层标题，两者必须一一对应（四级联动 L1↔L2）
6. 撰写参考答案：≥1000字，有开篇背景段和结尾升华段，禁用AI腔；顶层标题须对应第4步板块并标注分值（见"分值三级联动约束"）
7. 校验子论点分值之和=板块满分

**【踩分点设计原则】（新增·2026-08-30）**

参考答案的踩分点设计必须遵循以下约束，确保评分标准稳定、可重复：

1. **核心史实原则**：每个踩分点只考察"核心史实+关键制度名称"，不考察细节
2. **禁止考察的过度细节**：
   - 战役战术细节（如长筱之战的铁丝链连射手）
   - 政策全称（如《武家诸法度》应简化为"参勤交代制"）
   - 非标志年份（如1575年、1588年，只保留1603年、1467年等标志性年份）
   - 制度目的说明（如"消耗大名财力、防止叛乱"不作为独立踩分点）
   - 人物具体事迹（如某大名的治理措施，除非是题目核心人物）
3. **丰富性约束**：参考答案应比学生答案更丰富、更有深度，但丰富体现在"展开论述"而非"增加细节要求"
4. **严格度自检**：生成完成后自查——如果参考答案要求答出学生答案中明显没有的冷门细节，应退回简化

**【核心考点与扩展考点区分】（新增·2026-08-31）**

1. **核心考点**：题目设问直接要求的，缺失会严重影响得分
   - 例如：德国分裂原因题，政治制度是核心考点
   - 评分原则：按踩分点分值正常扣分
2. **扩展考点**：题目未直接要求，但可以作为加分项
   - 例如：德国分裂原因题，经济、文化是扩展考点
   - 评分原则：扩展考点缺失不扣分或只扣1-2分（作为整体评价参考）

**【踩分点结构规范】（新增·2026-08-31）**

> **为什么有这一节**：同一道"中世纪德国未能形成统一国家的原因"题，四次独立批改的要点得分为 16 / 19 / 21 / 16，极差 5 分。把四次结果统一到 10 个语义知识点后做方差分解：**固定判读只让结构变 → 极差 8.54 分；固定结构只让判读变 → 极差 3.95 分。结构差异占 68%，是判读差异的 2.16 倍。** 以下三条规则针对结构方差，且已实测可拦截最严重的那部分。

**规则一：反复合点（1 个踩分点 = 1 个知识单元）**

一个踩分点只能对应一个可独立判分的知识单元，禁止把两个知识单元绑进同一个点。

**判定方法**：踩分点名称中出现 `与` / `及` / `、` / `和` 连接两个可独立判分的名词短语，即视为疑似复合点，须拆分或改写。

| 复合点（违规） | 拆分后（正确） | 问题 |
|---|---|---|
| 缺乏统一的经济基础与民族认同（2分） | 经济分散与市场割裂／民族认同缺失 | 经济与社会是两个单元，学生只答其一时无法区分给分 |
| 欧洲大国干涉与德皇南倾（5分） | 外部大国干涉（法国）／德皇南倾意大利 | 答法国或答南倾都能拿这 5 分，两个独立史实被绑死 |
| 选举制与《黄金诏书》法律化分裂（4分） | 皇位选举制／《金玺诏书》固化分裂 | 同上，且两个单元分值被摊薄 |

**为什么必须反复合点**：复合点会导致两种失真——学生答出其中一个单元就白拿满分，或答出一个单元却因另一个没答而被扣分。**同一次批改里，"经济分散"这个知识点因复合与否，实测分值在 1 分到 6 分之间摆动。**

**规则二：知识点维度组织（随题而选，参考 `references/维度参考.md`）**

**维度组织原则**：维度仅作组织答案的**参考工具**，题目自身优先，不强制套用、不强制全覆盖。

1. **按题目选择维度**：若题目适合按领域展开，从 `references/维度参考.md` 选用合适的维度组织踩分点（该文件按中国古代史 / 世界古代史 / 世界近现代史分多套维度，如政治制度、经济生产、社会阶层、思想文化、对外军事、史学发展、成就等，可按题取舍）；**若题目不适用维度法（偏科、专题、单点题），直接按题目逻辑展开，不套维度**。
2. **允许维度缺失**：某维度若题目本身根本未涉及，**不设该维度的踩分点**——"该维度无对应知识点"是正常现象，不是违规。反例：漏设的是题目**实际涉及**的内容才属问题。
3. **禁止为凑维度编造**：**禁止为凑齐维度比例而编造知识点**——那样比维度缺失更糟（反形式主义）。

**维度缺失的隐患**（仅当题目实际涉及、却被漏设时才构成问题）：该内容在评分标准里不存在，学生写了无分可给、没写也不扣分。实测中，批改①漏设"德皇南倾意大利"这一独立点，学生写了整整一段却无分可给——这是"题目涉及却漏设"，不是"维度缺失"。

**反形式主义约束（重要）**：本规则不要求各维度分值均等，也不要求维度数量固定。偏科题目（如纯政治制度题）只设题目实际涉及的维度即可。

**规则三：参考答案独立三遍生成 + 多数合并（取代"锚库复用"）**

> 本规则废弃早期"批改完成后导出锚文件、下次复用"的做法。参考答案是整个评分链路的最大方差源（占 68%，见本节开头方差分解）——**不再依赖落库复用，改为每次批改前独立生成三遍、多数合并取共识**，既压方差，又不引入锚文件的维护负担。

1. **先识别题目是否新题**（保留三元组判断精神）：提取「时间范围 + 对象 + 设问」，三者一致即同一题
   - `15世纪中期至17世纪中期日本政治体制的演变及影响` ≠ `论述日本政治体制演变`（前者有精确时间限定，不是同一题）
   - `论述中世纪德国未能形成统一国家的原因` = `论述中世纪德国分裂的原因`（措辞微调，同一题）
   - 同一题在三遍生成前**先复用上次已生成的参考答案**，只重新生成学生得分侧；新题则执行下方三遍协议
2. **三遍独立起草**：不查看学生答案，从题目出发**隔离**生成三版参考答案骨架（板块划分 + 知识点清单 + 建议分值）。每版起草前不得回看前版，避免锚定效应
3. **多数合并取共识**：把三版统一到同一组语义知识点（执行AI完成语义归组），统计每个知识点出现次数：
   - 出现 **≥2 次** → 保留，分值取三版中位数
   - 仅出现 **1 次** → 丢弃（单次心证不可靠），除非它是维度全覆盖（规则二）所必需的最小点
4. **结构定版**：板块数 = 题目子问项数、单板块 ≤18、各板块满分之和 = 30，中位数之和与目标不符时按比例缩放、四舍五入到 0.5 的倍数
5. **正文撰写**：以合并后的骨架为准，撰写参考答案正文（≥1000字，开篇背景段 + 结尾升华段，禁用AI腔）并标注各板块分值

**为什么必须三遍**：单遍生成的知识点清单随机性高（同题四遍实测要点重合率不足一半），三遍取多数是成本最低的收敛手段。多数合并只对"主流知识点"收敛，边缘知识点允许取舍，但禁止为凑整齐而编造知识点（呼应反形式主义约束）。

**【违规案例】**
| 过度严格（违规） | 合理约束（正确） | 问题 |
|----------------|----------------|------|
| 要求答出长筱之战（1575年）及铁丝链连射手战术 | 只要求答出织田信长武力统一日本 | 战役细节超出统考范围 |
| 要求答出《武家诸法度》全称及具体内容 | 只要求答出参勤交代制 | 政策全称非核心考点 |
| 要求答出元和偃武、260年和平等结论 | 只要求答出结束分裂、维持稳定 | 结论性表述可作为参考答案内容，但不作为独立踩分点 |
| 要求答出乐市乐座、分国法、知行制度等战国经济制度 | 只要求答出统一事业推动 | 经济制度细节非政治体制题核心考点 |

**【输出格式】**
```
  开篇背景段（首行缩进两格）

一、XX（X分）

  （一）XXX（X分）
    正文内容...

  （二）XXX（X分）
    正文内容...

二、XX（X分）
  ……

  结尾升华段（首行缩进两格）
```

**【检查点】**
- [ ] 字数≥1000字
- [ ] 每个板块下的子论点分值之和 = 板块满分
- [ ] 无AI腔禁用词
- [ ] 无英文残留
- [ ] 有开篇背景段和结尾升华段
- [ ] 参考答案比学生答案更丰富、更有深度
- [ ] **踩分点只考察核心史实+关键制度名称，无过度细节**（新增）
- [ ] **自查：无战役战术、政策全称、非标志年份等超出统考范围的细节要求**（新增）
- [ ] **无复合踩分点**：一个点只含一个可独立判分的知识单元，名称不含"与/及/、"连接的双单元（新增·2026-08-31）
- [ ] **维度组织随题而选、允许缺失**：按题目从 `references/维度参考.md` 选维度；题目未涉及的维度不设踩分点，偏科/专题/单点题直接按题目逻辑展开，不套维度；**未为凑维度编造知识点**（新增·2026-08-31）
- [ ] **已完成三遍独立生成 + 多数合并**：参考答案骨架来自 ≥2 遍共识，非单遍心证（新增·2026-08-31）
- [ ] **三遍草稿已落盘 `three_passes` 字段**：批改 JSON 须含 `three_passes`（三遍骨架列表，每遍=板块列表，板块含 `name` 与 `points:[{name,score}]`），供 grade_checker 的 `check_three_passes()` 校验共识实质（新增·2026-09-01）
- [ ] **同题已复用上次参考答案**：若为新题，走三遍协议；若为旧题，已复用上次参考答案仅重生成得分侧（新增·2026-08-31）
- [ ] **全部引用相对路径**：本 skill 内任何文件引用（维度参考、脚本、模板等）一律用相对路径（以 skill 目录为基准），不得写死 `C:/Users/妄/...` 等绝对路径（新增·2026-08-31）

**【脚本实现】** `generate_answer.py` — 提供 `extract_question_triple()`, `is_same_question()`, `merge_passes()`, `merge_and_output()` 四个函数。AI 调用方式为：先调 `is_same_question()` 判断是否旧题，旧题直接复用 JSON 中已存储的参考答案；新题则手动执行三遍独立生成（每次隔离上下文，不回顾前版），将三遍结果以列表形式传入 `merge_passes(passes, question)` 得到收敛后的骨架，再据此撰写正文参考答案。输出骨架结构：`[{'name': str, 'max_score': float, 'points': [{'name': str, 'score': float}]}]`。

**【`three_passes` 字段格式（须写入批改 JSON）】** 供 `grade_checker.check_three_passes()` 校验共识实质。结构为「3遍 × 板块列表」，每遍是一个板块列表，板块含 `name` 与 `points`（知识点列表，每个知识点为 `{'name': str, 'score': float}`）：
```
three_passes = [
  [ {"name":"主要内容","points":[{"name":"中央集权官僚体制","score":4}]}, {"name":"历史影响","points":[{"name":"推动封建化","score":3}]} ],
  [ {"name":"主要内容","points":[{"name":"中央集权官僚体制","score":3.5}]}, {"name":"历史影响","points":[{"name":"推动封建化","score":2.5}]} ],
  [ {"name":"主要内容","points":[{"name":"中央集权官僚体制","score":4}]}, {"name":"历史影响","points":[{"name":"推动封建化","score":3}]} ],
]
```
校验器按板块位置对齐，检查每个最终踩分点是否在 ≥2 遍草稿中找到语义匹配（用 `semantic_equal`）。若某个最终踩分点仅在 <2 遍出现，报警提示可能被单遍幻觉引入（新增·2026-09-01）。
**【自检器检查】** 全局自检D项（参考答案自检）+ G项（踩分点严格度自检，新增）
**【违规案例】**
| 违规行为 | 正确做法 | 违规后果 |
|---------|---------|---------|
| 查看学生答案后再生成参考答案 | 先生成参考答案，再查看学生答案 | 参考答案被带偏，踩分点不全 |
| 跳过参考答案生成 | 参考答案是评分依据，不可省略 | 评分无标准，失去客观性 |

**【常见错误】**
| 错误 | 后果 | 避免方法 |
|------|------|---------|
| 子论点分值之和不等于板块满分 | 评分失真 | 生成后立即校验求和 |
| 字数不足1000字 | 内容不够深入 | 关键史实详细展开，不简写 |
| 套维度模板牵强附会 | 结构不符合题目逻辑 | 先判断维度套用是否合理 |

**【示例】**
```
  中世纪德意志长期处于分裂状态，未能形成统一的民族国家，这是其历史发展的核心特征。这一结果并非由单一因素造成，而是政治、经济、社会、宗教与外部环境等多重因素交织作用的产物。

一、政治层面（12分）

  （一）皇位选举制导致皇权先天不足（4分）
    神圣罗马帝国的皇帝由诸侯选举产生，每到皇位更替，选帝侯便向新皇帝索取大量特权作为支持条件，导致皇权随每一次选举而不断削弱。1356年查理四世颁布《金玺诏书》，从法律上正式确认七大选帝侯选举皇帝的制度，并规定选帝侯在其领地内享有行政、司法、铸币、采矿等完全特权。这标志着德意志分裂割据的合法化。

  （二）皇权与教权长期斗争，两败俱伤（4分）
    自奥托一世建立神圣罗马帝国起，德皇便与教皇展开漫长的叙任权斗争。1077年"卡诺莎事件"后，德皇在与教皇的对抗中长期居于下风。《沃尔姆斯宗教协定》（1122年）规定德国主教一律依照教会法选举，皇帝的神职授予权被严重削弱。教皇为保持自身在德国的利益，常与帝国内部诸侯联手反抗皇帝。这场旷日持久的政教之争使皇权元气大伤，而地方诸侯则趁机坐大。

二、经济层面（6分）
  ……

  总之，中世纪德国未能形成统一国家，是由初始制度缺陷（选举制）引发，因战略失误（南倾意大利）而加剧，被法律文件（《金玺诏书》）所固化，并因经济分散、民族认同缺失和外部干预而变得积重难返的复杂过程。
```

---

## 第三步：从参考答案提取踩分点（阶段一·独立生成）

**【输入要求】**
- 第二步生成的参考答案

**【处理规则】**
1. 按参考答案自然结构提取，不按固定维度拆分
2. 每个要点标注：核心关键词、重要性（核心/重要/补充）、建议分值
3. 核心要点占板块总分30-40%，次要要点占10-20%
4. "维度"列填具体分析维度（政治/经济/军事等），非板块名称；若按自然结构分组则填"—"
5. 踩分点不重叠，如存在交叉需合并或明确区分边界
6. **【新增】严格度校验**：每个踩分点的核心关键词不超过5个；不得包含战役战术、政策全称、非标志年份等过度细节
7. **【新增】踩分点列格式**：踩分点列必须写具体的踩分点名称（如"皇位选举制导致皇权先天不足"），禁止只写重要性标签（如"核心""重要"）
8. **【新增】禁止重复踩分点**：同一内容不应在多个踩分点中重复出现

**【输出格式】**
| 踩分点 | 核心关键词 | 重要性 | 建议分值 | 维度 |
|--------|-----------|--------|---------|------|

**【检查点】**
- [ ] 所有踩分点分值之和 = 30分
- [ ] 各板块踩分点分值之和 = 板块满分
- [ ] 踩分点之间无内容重叠
- [ ] 维度列填写正确（非板块名称）
- [ ] **踩分点核心关键词不超过5个，无过度细节**（新增）
- [ ] **踩分点列写具体名称，不写重要性标签**（新增）
- [ ] **无重复踩分点**（新增）

**【脚本实现】** `extract_scoring_points()` → 调用 `check_total_score()`, `check_overlap()`, `check_scoring_detail_level()`（新增）
**【自检器检查】** 全局自检C项（分数一致性自检）+ G项（踩分点严格度自检，新增）
**【违规案例】** 无
**【常见错误】**
| 错误 | 后果 | 避免方法 |
|------|------|---------|
| 按固定维度拆分 | 忽略参考答案自然结构 | 按参考答案子论点顺序提取 |
| 踩分点之间内容重叠 | 学生答题时混淆 | 明确各踩分点边界，重叠则合并 |
| 分值总和不等于30 | 评分标准失效 | 生成后强制校验求和 |
| 踩分点包含过度细节 | 评分标准过严，同份答案分数波动大 | 生成后执行严格度自检，超标则简化 |
| 踩分点列只写重要性标签 | 格式错误，学生无法理解 | 踩分点列必须写具体名称 |
| 重复设置踩分点 | 评分标准混乱，同一内容重复扣分 | 生成后执行去重检查 |

---

## 分值三级联动约束（新增·2026-08-30）

**原则：同一道题的分值必须在四个层面严格一致，不允许出现任何层面的脱节。**

### 层级定义

| 层级 | 内容 | 约束 |
|------|------|------|
| **L1 参考答案** | 板块标题 `一、XX（X分）` + 子论点 `（一）XXX（X分）` | 子论点分值之和 = 板块满分；板块满分之和 = 30；**单板块≤18**；板块数 = 题目子问项数 |
| **L2 踩分点表** | 每个踩分点 `max_score` 列 | 踩分点满分之和 = 板块满分；L2总分 = L1总分 |
| **L3 学生得分列** | 每个踩分点 `score` 列 | 学生得分之和 = 板块得分；不得与板块得分冲突 |
| **L4 顶层汇总** | `points_score` + `language_score` = 总分（卷面整洁已并入 `language_score` 六维之和）；诊断文本分数 = 总分概览合计 | 四层数值必须完全一致 |

### L1与L2的映射关系

**L1子论点与L2踩分点不必一一对应。** 允许以下两种映射模式：

| 映射模式 | 说明 | 约束 |
|---------|------|------|
| **1对1** | 一个L1子论点对应一个L2踩分点 | 分值直接相等 |
| **1对多** | 一个L1子论点涵盖多个L2踩分点 | L1子论点的分值 = 涵盖的所有L2踩分点max_score之和 |

**校验要求**：每个L2踩分点必须能被归入某个L1子论点，不能存在悬空的L2踩分点。

### 全量校验规则

```
L1板块一满分    = L1板块一子论点分值之和 = L2板块一max_score之和
L1板块二满分    = L1板块二子论点分值之和 = L2板块二max_score之和
要点总分(30)    = 板块一满分 + 板块二满分
L3要点学生分    = 板块一学生得分 + 板块二学生得分 = 各踩分点score之和
论述组织分(10)  = 六维度得分之和（结构+逻辑+史论+文字+史观+卷面整洁/neatness）
                  其中 language_score = 六维 score 之和（卷面整洁已作为 neatness 维度计入，顶层不再单独加，须置 0）
L4总分          = L3要点学生分 + language_score
诊断文本分数    = L4总分（必须完全一致）
总分概览合计    = L4总分（必须完全一致）
```

### 输出约束

- **参考答案**：板块标题和每个子论点末尾必须标注 `（X分）`，例如 `一、XX（20分）`、`（一）XXX（4分）`
- **踩分对照表**：学生作答列必须摘录关键句（核心句摘录，一般不超过120字；超120字视为疑似粘贴整段原文，应精简），不得粘贴整段原文
- **对照表"得分/满分"列**：必须同时显示学生得分和该要点满分，格式为 `3/4`

---

## 阶段一完成检查（强制暂停点）

**在完成第四步之前，必须确认以下事项：**
- [ ] 所有题目的参考答案已生成
- [ ] 所有题目的踩分点已提取
- [ ] **未查看任何学生答案图片**
- [ ] 参考答案与题目一一对应

**只有以上四项全部确认，才能继续执行第四步。**

**批量模式特别说明**：若有多道题，必须完成所有题目的第二步和第三步，才能进入阶段二。

---

## 第四步：OCR识别学生答案（阶段二·对照评分）

**【输入要求】**
- 学生答案图片

**【处理规则】**
1. 输出【阶段隔离承诺书】
2. 优先使用视觉功能，置信度<80%转用OCR skill
3. 低置信度段落（<70%）高亮标注"⚠️疑似OCR识别异常"
4. 仅对史实硬伤标红（人名、时间、事件、制度名称错误），表述不规范/语病放入【本题诊断】不标红
5. 标红后用括号注释正确答案，如"奥托一世（应为亨利四世）"

**【输出格式】**
- OCR识别文本（史实硬伤标红加粗，括号注释正确答案）
- 末尾注明："注：标红处为史实错误，括号内为正确表述。"

**【检查点】**
- [ ] 所有学生答案图片已完成OCR识别
- [ ] 标红内容仅限于史实硬伤
- [ ] 低置信度段落已标注"⚠️疑似OCR识别异常"
- [ ] 学生答案末尾有标红说明

**【脚本实现】** `ocr_student_answer()` + `red_marker()` → 调用 `check_red_scope()`
**【自检器检查】** 全局自检A项部分（学生作答列格式）
**【违规案例】** 无
**【常见错误】**
| 错误 | 后果 | 避免方法 |
|------|------|---------|
| 把语病、表述不规范也标红 | 标红范围过宽，干扰学生 | 仅标红人名、时间、事件、制度名称错误 |
| 低置信度段落未标注 | 学生可能被错误OCR结果误导 | 对<70%置信度段落标注"⚠️疑似OCR识别异常" |

---

## 第五步：逐点评分（踩分点对照表）（阶段二·对照评分）

**【输入要求】**
- 第四步OCR识别的学生答案
- 第三步提取的踩分点

**【处理规则】**
1. 使用五档达成度比例法评估每个踩分点的达成度（完整100%/基本75%/部分50%/触及25%/未答0%），据此定档；最终得分按第2条归入 0.5 倍数
2. **得分精度必须为 0.5 的倍数**：每个踩分点的学生得分必须落 0/0.5/1/1.5/2… 这类 0.5 步长取值，**禁止出现 3.2/4、2.7/3 等任意小数得分**。0.5 步长既兼容五档比例（满分4分时 3.5/4=87.5% 属正常细粒度），也兼容论述组织表的 0.5 扣分步长（1.5/2），两表统一按"0.5 倍数"校验，不强制五档百分比。若算出非 0.5 倍数值，须重新审视该点达成度，归入最接近且符合实际的 0.5 步长档
3. 史实硬伤（人名/时间/事件张冠李戴）降一档评分；制度名称近似表述可维持原档
4. 点评必须包含以下要素：
   - **答出了什么**：引用学生答案原文，说明答出了哪些内容
   - **哪里有问题**：指出具体问题（如有）
   - **正确表述**：给出改进方向（如有）
   - **满分点评**：如果学生答满分，只需说明"答出了什么"，无需"哪里有问题"和"正确表述"
4. 点评为自然语言一段话，流畅连贯像老师口头点评
5. **禁止使用内部评级词汇**：不得使用"完整命中""基本命中""触及边缘""部分命中"等内部评级词汇
6. 学生作答列直接摘录关键表述，无引导语、无双引号，未涉及写"无"

**【优质点评示例】（来自报告①）**
```
答出了选举制导致皇权弱化的核心逻辑，提及《黄金诏书》的法律作用，但缺少1356年查理四世这一标志性信息，且未展开阐述诏书具体内容，扣1分。正确表述：1356年查理四世颁布《金玺诏书》，确认七大选帝侯选举皇帝的制度，并赋予诸侯领地内近乎完全的自治权，从法律上固化了分裂格局。
```

**【错误点评示例】**
```
❌ "完整命中：给满分。"（过于简略，缺少具体内容）
❌ "基本命中：扣1分。"（缺少答出了什么和正确表述）
❌ "完整命中：答出...给满分。"（使用了内部评级词汇"完整命中"）
```

**【输出格式】**
```
【踩分点对照表】

（一）XX（满分X分，得分X分）
| 踩分点 | 学生作答 | 得分/满分 | 点评 |
|--------|---------|----------|------|
```

**【检查点】**
- [ ] 每个踩分点点评为自然语言一段话，无【】标签
- [ ] 点评包含四要素：答出了什么、哪里有问题、扣分、正确表述
- [ ] 学生作答列无引导语、无双引号
- [ ] 各板块内学生各点得分之和 = 板块得分
- [ ] 各板块踩分点分值之和 = 板块满分

**【脚本实现】** `score_points()` → 调用 `check_comment_format()`, `check_student_answer_format()`, `check_section_score_sum()`
**【自检器检查】** 全局自检A项完整 + E项（点评格式自检） + C项部分
**【违规案例】**
| 违规行为 | 正确做法 | 违规后果 |
|---------|---------|---------|
| 点评写成填表格式（带【】标签） | 点评应为自然语言一段话，流畅连贯 | 输出僵硬，不符合规范 |
| 史实硬伤未降一档评分 | 人名/时间/事件错误必须降档 | 评分偏宽松，失去客观性 |
| 学生作答列加引导语或引号 | 直接摘录关键表述，不加任何前缀 | 格式不规范 |

**【常见错误】**
| 错误 | 后果 | 避免方法 |
|------|------|---------|
| 凭感觉打分不用五档比例法 | 评分主观，失去客观性 | 严格按五档比例法计算 |
| 点评缺少四要素之一 | 评分理由不充分 | 生成后逐项检查四要素 |

**【示例】**
```
完整命中：完整答出八柱国、大将军、开府、二十四军、府兵不入民籍等核心内容，表述准确，给满分。
基本命中：答出卡诺莎事件和教皇与德皇争斗，但人名错误（奥托一世→亨利四世），属基本命中，扣1分。正确表述：1077年，德皇亨利四世前往意大利卡诺莎向教皇格里高利七世请求赦免。
部分命中：仅提到金玺诏书名称，未提及1356年、查理四世、诸侯特权等关键内容。正确表述：1356年查理四世颁布《金玺诏书》，正式确认七大选帝侯选举皇帝的制度，并赋予诸侯领地内近乎完全的自治权。
触及边缘：仅提到"教皇干涉"四字，几乎未展开。正确表述：教皇长期干涉德意志统一，因德意志分裂有利于教皇控制德国，且德国被称为"教皇的奶牛"。
未答：未涉及社会层面的分裂原因，全扣。正确表述：经济分散、城市势力软弱、关税林立、货币不统一等经济因素。
```

---

## 第六步：论述组织评分（10分）（阶段二·对照评分）

**【输入要求】**
- 第四步OCR识别的学生答案

**【处理规则】**
1. 六维度逐项独立评分（整体结构2/论证逻辑2/史论结合2/文字表达1/史观运用1/卷面整洁2），各维度评分标准见下方【六维度评分标准】
2. 评语须符合【点评格式标准】：先肯定整体表现、引用学生答案具体句子/术语为证，再用"但"转折指出不足（若有），全程自然语言，禁止关键词罗列
3. 评分理由严格对照学生答案实际内容，禁止模板化套用，不得出现与学生答案矛盾的表述

**【六维度评分标准（固化·2026-08-31）】**

每个维度按"满分表现 → 扣减情形"两级把握，避免凭感觉打分：

| 维度 | 满分 | 考察内容 | 评分标准（满分表现 → 扣减情形） |
|------|------|---------|-------------------------------|
| 整体结构 | 2 | 框架完整性、层次分明度、板块/段落是否对应设问 | 2分：总分总或清晰分段，板块对应设问，层次分明，标题规范 → 扣：分段标题不规范、编号体例不统一、影响段与前文冲突（扣0.5） |
| 论证逻辑 | 2 | 时序/因果链条清晰度、论证递进与无矛盾 | 2分：按时序或逻辑链展开，因果成立，过渡自然 → 扣：部分过渡生硬、论证深度不足（扣0.5） |
| 史论结合 | 2 | 用具体史实支撑观点，且史实与结论有内在逻辑联系 | 2分：有史实且能深入分析史实与结论的逻辑 → 扣：偏罗列史实、简单贴结论标签、缺乏深入分析（扣0.5–1） |
| 文字表达 | 1 | 术语准确性、语句流畅度、笔误/语病 | 1分：术语准确、语句通顺、无明显笔误 → 扣：人名颠倒等笔误、语病、口语化（扣0.5） |
| 史观运用 | 1 | 历史视角（唯物史观/文明史/全球史）、多维度分析 | 1分：能从合适史观切入、涉及多维度、有宏观视角 → 扣：仅有基本视角但缺更宏观分析（扣0.5） |
| 卷面整洁 | 2 | 字迹、段落、标点 | 2分：字迹清晰、段落分明、标点规范 → 扣：涂改/标点混乱（扣1）、难以辨认（扣2） |

**【点评格式标准（固化·2026-08-31）】**

每个维度的评语必须遵循"先扬—引证—后抑"结构，且全程引用学生答案中的**具体句子或术语**作为证据，禁止空泛套话与关键词罗列：

1. **先扬**：点明该维度的整体表现（肯定为主），如"采用'总—分—总'结构，框架合理"。
2. **引证**：引用学生答案中的具体句子、编号或术语为据，如"'二、'后直接接段落无标题""'织田长信'应为'织田信长'""'对对外文化贸易'语病"。
3. **后抑**：若有不足，用"但"转折指出，同样**引用具体原文**指明问题，如"但分段标题不够规范（'二、'后直接接段落无标题）"。
4. **自然语言**：一段话流畅连贯，像老师口头点评；禁止"层次分明，段落对应设问"式的关键词罗列。

**【优质示例（来自报告①，可直接套用此格式）】**
```
【论述组织评分】（满分10分）
| 维度 | 得分/满分 | 评语 |
|------|----------|------|
| 整体结构 | 1.5/2 | 采用'总—分—总'结构，先概述四阶段再分三个时期详述最后写影响，框架合理。但分段标题不够规范（'二、'后直接接段落无标题），且影响部分用'（二）'编号与前文不统一 |
| 论证逻辑 | 1.5/2 | 按时序展开（室町→织田→丰臣→德川→影响），逻辑链条清晰。因果关系基本成立（如'经济发展推动统一'）。但部分过渡略显生硬，论证深度不足 |
| 史论结合 | 1/2 | 有史实支撑论述（应仁之乱、太阁检地、刀狩令等），但史论结合偏弱——多为罗列史实后简单贴结论标签，缺乏深入分析史实与结论之间的内在逻辑联系 |
| 文字表达 | 0.5/1 | 整体通顺可读，但存在明显笔误（'织田长信'应为'织田信长'）；部分语句表达欠流畅（'对对外文化贸易'语病）；个别措辞口语化倾向 |
| 史观运用 | 1/1 | 能从政治体制演变角度切入分析，涉及阶级、经济、对外关系等多维度，具备初步的唯物史观意识。但缺乏更宏观的文明史或全球史视角 |
| 卷面整洁 | 2/2 | 字迹工整清晰，段落分明，标点使用规范，卷面整洁 |
| 合计 | 7.5/10 | |
```

**【检查点】**
- [ ] 六维度均有独立评分，未合并给分
- [ ] 评语为自然语言，非关键词罗列
- [ ] 每个维度评语均引用学生答案具体句子/术语为证
- [ ] 评语遵循"先扬—引证—后抑"结构（不足处用"但"转折并引用原文）
- [ ] 评语与学生答案实际内容一致，无矛盾表述
- [ ] 六维度得分之和 = 论述组织（含卷面）得分

**【脚本实现】** `score_language()` → 调用 `check_six_dimensions()`, `check_language_natural()`, `check_language_review_format()`（新增）
**【自检器检查】** 第六步新增 `check_language_review_format()`（评语非空、非关键词罗列、须含句号或转折词，否则报错/警告）
**【违规案例】**
| 违规行为 | 正确做法 | 违规后果 |
|---------|---------|---------|
| 六维度合并给一个分数 | 每个维度独立评分 | 失去分项诊断价值 |
| 评语用关键词罗列 | 用自然语言描述，像口头点评 | 像AI报告，不符合规范 |
| 评语与学生答案矛盾 | 评分前重新阅读学生答案 | 学生困惑，批改失效 |

**【常见错误】**
| 错误 | 后果 | 避免方法 |
|------|------|---------|
| 模板化套用评语 | 每个题评语雷同 | 严格对照学生答案实际内容 |
| 漏评某个维度 | 诊断不完整 | 六维度逐项检查 |

---

## 第七步：本题诊断（阶段二·对照评分）

**【输入要求】**
- 第五步、第六步评分结果
- 第四步OCR识别的学生答案

**【处理规则】**
1. **亮点**：引用学生答案中的具体句子+说明为什么好
2. **不足**：只写具体问题，不写正确表述（正确表述留给踩分点点评）
3. **改进方向**：给出方向指引，不给详细操作步骤
4. **整体点评**：七要素齐全，用完整句子表述，禁止括号内嵌理由

**【输出格式】**
```
一、亮点
• [具体句子] + [为什么好]

二、不足
• [具体问题]

三、改进方向
• [方向]

【整体点评】
[七要素齐全的段落，无括号内嵌理由]
```

**【检查点】**
- [ ] 亮点引用了学生答案中的具体句子
- [ ] 不足只写问题不写正确表述
- [ ] 整体点评七要素齐全（类卷等级及理由、得分预估、要点踩分情况、组织论述情况、亮点、不足、提分空间）
- [ ] 整体点评无括号内嵌理由
- [ ] 整体点评分数与总分概览一致

**【脚本实现】** `generate_diagnosis()` → 调用 `check_highlight_citation()`, `check_diagnosis_elements()`, `check_no_parenthetical_reasoning()`
**【自检器检查】** 全局自检B项完整（整体点评自检）
**【违规案例】**
| 违规行为 | 正确做法 | 违规后果 |
|---------|---------|---------|
| 亮点无具体句子引用 | 必须引用学生原句+说明为什么好 | 泛泛而谈，学生不知道好在哪里 |
| 不足里塞正确表述 | 不足只写问题，正确表述放踩分点点评 | 与踩分点点评重复 |
| 整体点评用括号内嵌理由 | 用完整句子表述判定依据 | 格式不规范 |

**【常见错误】**
| 错误 | 后果 | 避免方法 |
|------|------|---------|
| 亮点泛泛而谈 | 学生不知道哪里好 | 必须引用学生原句 |
| 整体点评漏要素 | 信息不完整 | 按七要素清单逐项检查 |

**【示例】**
```
一、亮点
• 政治困境分析较好："两极格局瓦解后，南方国家难以通过依靠美苏两强的任一获得政治经济援助，因此其国际地位下降"——表述准确，史实清晰。
• 结构完整：总分总结构清晰，开头有概述，结尾有总结。

二、不足
• 史实名称错误："上海综合组织"表述错误。
• 社会困境完全缺失：学生答案只涉及政治、经济两个维度，社会困境完全未提及。
• 发展部分维度不全：学生答案只提到南南合作、发展道路、全球治理三个维度，缺少新兴经济体崛起这一关键点。

三、改进方向
• 准确记忆国际组织名称——上海合作组织（SCO，2001年）、金砖国家（BRICS，2009年首次会晤）、东盟（ASEAN，1967年）
• 困境分析要覆盖政治、经济、社会三维度——政治（格局瓦解、矛盾爆发、外部干涉）、经济（依赖出口、规则不公、差距扩大）、社会（贫富分化、贫民窟、难民）缺一不可
• 史论要结合——每个观点都要有具体史实支撑，如1997年亚洲金融危机而非笼统说经济危机爆发

【整体点评】
本题得分为26.5/40，内容较完整但有多处史实缺失，分析偏于表面，属三类卷。学生掌握了基本时间脉络，结构较为完整；主要失分在于某部分几乎空白，影响分析深度不足。若能在具体措施记忆和影响分析上加以补足，有望达到二类卷水平。
```

---

## 输出规范

### 学生反馈Word（最终交付物）

**命名规则**：`{题目关键词}_批改报告.docx`

**类卷等级标准**：
| 等级 | 分数段 | 说明 |
|------|--------|------|
| 一类卷 | 35-40分 | 内容完整，史实准确，分析深入，表达流畅 |
| 二类卷 | 28-34分 | 内容较完整，有史实错误但不影响整体，分析较深入 |
| 三类卷 | 20-27分 | 内容不完整，有多处史实错误，分析较浅 |
| 四类卷 | 0-19分 | 内容严重缺失，大量史实错误，难以成文 |

**报告结构**：
```
【封面/标题区】标题 + 题目原文

【总分概览】要点踩点X/30 + 论述组织X/10 + 合计X/40

【学生答案】OCR文本（史实硬伤标红加粗，括号注释正确答案）+ 末尾标红说明

【踩分点对照表】按板块分组，每组标题「（一）XX（满分X分，得分X分）」
  | 踩分点 | 学生作答 | 得分/满分 | 点评 |

【论述组织评分】六维度表格

【本题诊断】亮点 + 不足 + 改进方向 + 整体点评

【参考答案】开篇背景段 → 一、XX（X分）→ （一）XXX（X分）→ 正文缩进 → 结尾升华段
```

**页脚**：居中，灰色小字 —— 「免费煎蛋卷（313版）」制作 ——

**语言风格**：像老师批语而非AI报告，短句为主，批评时指向具体句子，禁用AI腔

**【脚本实现】** `generate_word_report()`
**【自检器检查】** 全局自检C项完整（分数一致性）

### 输出路径配置（可配置 · 2026-09-01 新增）

报告默认保存在 **skill 目录内的 `outputs/`**（以 skill 根为基准，不依赖运行时的当前工作目录 CWD），保证分享给他人后落点稳定可预期。

**自定义保存位置（两种方式，可叠加）：**
- **持久配置**：编辑 skill 根目录的 `config.json`（可参考 `config.example.json`），设 `"output_dir"`：
  - 留空 `""` → 用默认 `skill/outputs/`
  - 填绝对路径 → 固定存该目录
  - 填相对路径 → 以 skill 根为基准解析（如 `"../reports"`）
  - 文件名由 `"filename_template"` 控制（默认 `{topic}_{timestamp}.docx`，`{topic}` 取题目清洗后前20字）
- **单次覆盖**：命令行用 `-o/--output` 指定完整路径（绝对或相对 CWD，优先级最高）

**命令行用法：**
```
python scripts/generate_word.py <输入.json> [-o 输出.docx] [--config 自定义config.json]
```

**优先级链**：`CLI -o` > `config.json.output_dir` > 默认 `skill/outputs/`

> **【强制】生成完成后必须告知用户路径**：Word 报告生成后，AI **必须在给用户的回复中用自然语言明确写出报告的完整保存路径**（例如「报告已保存到：`C:\Users\某\.workbuddy\skills\essay-grader\outputs\题目名_时间戳.docx`」）。脚本虽会在控制台打印路径，但控制台输出用户未必会看；必须转述到对话里，否则用户找不到文件。路径取脚本实际落盘的真实物理路径（junction 入口与真源路径指向同一文件，写任一个即可，但推荐写用户实际使用的入口路径）。

---

## JSON 数据契约（必读 · 生成层与验证层/执行层一致）

> `grade_checker.py`（验证层）与 `generate_word.py`（执行层）**都按以下格式消费数据**。AI 在生成批改 JSON 时必须严格遵循，否则校验器报错或渲染错乱。这是三位一体的规范层约束——务必钉死，不要靠记忆。

**顶层字段：**
```json
{
  "question": "题目文本",
  "student_answer": "学生答案（可含 \\n\\n 分段；史实硬伤由 red_keywords 标红）",
  "reference": "参考答案（≥1000字，无AI腔，首末段缩进两格）",
  "sections": [ "板块对象，见下" ],
  "language_breakdown": { "六维度论述组织评分，见下" },
  "language_score": 10.0,
  "points_score": 30.0,
  "total_score": 40.0,
  "diagnosis": "整体点评文本（必须是字符串，不是字典）",
  "issues": [ {"type":"亮点","problem":"..."}, {"type":"不足","problem":"...","fix":"正确表述"} ],
  "suggestions": [ {"type":"改进方向","problem":"..."} ],
  "red_keywords": [ ["错误词","正确词"] ],
  "three_passes": [ "三遍生成记录，可选" ]
}
```
- `language_score` = `language_breakdown` 六维 `score` 之和（满分10）；`points_score` = 各 `section.score` 之和（满分30）。二者校验器会核对，缺失按 0 算并报错。
- `total_score` = `points_score` + `language_score`（满分40）。**校验器强制核对**：若与分项之和不符，直接报 error 并阻断生成（防手算笔误，如写成 20.5 实为 22）。渲染层总分概览也以此为准重算，不依赖该字段。
- `student_answer` **必填**（顶层字符串字段）。`generate_word.py` 的 `set_grading_result` 会读取它渲染到【学生答案】区；若缺失/为 None，渲染层兜底显示"未提供学生答案"（绝不会再出现字面量 `None`）。AI 生成 JSON 时切勿遗漏此字段。
- `issues` 推荐提供（含至少一个 `type:"亮点"`），否则校验器提示"亮点为空"。`suggestions` / `red_keywords` / `three_passes` 可选。

**渲染层表格列宽（固定，不随内容伸缩）：**
- 【踩分点对照表】四列宽度（厘米）：踩分点名称 2.8 / 学生作答 3.0 / 得分满分 1.2 / **点评 8.5（最宽）**。
- 【论述组织评分】三列宽度（厘米）：维度 2.5 / 得分满分 1.5 / **评语 11.5（最宽）**。
- 原因：点评/评语文本最长，固定最宽列避免被挤压成一行几字、阅读困难。如需调整，改 `generate_word.py` 中 `_set_table_column_widths` 的传参。

**板块（sections 元素）：**
```json
{
  "name": "政治制度的延续与发展",
  "max_score": 14,   // 板块满分（≤18，所有板块之和须=30）
  "score": 9.5,      // 板块学生得分（= 其下 points 各 [5] 之和）
  "points": [ "7元素数组，见下" ]
}
```

**踩分点（points 元素 = 7 元素数组，顺序不可乱）：**
```
[ 名称, 关键词, 学生作答, 满分, 维度, 得分, 点评 ]
  [0] 名称      string  踩分点具体名称（禁止只写"核心/重要"等标签，空名报错）
  [1] 关键词    string  逗号分隔的关键术语
  [2] 学生作答  string  与踩分点直接相关的核心句摘录（≤120字，无引导语/引号，未涉及写"无"）
  [3] 满分      number  该点满分（须为 0.5 倍数）
  [4] 维度      string  具体分析维度（禁止用板块名如"背景/措施/影响"；无则写"—"）
  [5] 得分      number  学生得分（须为 0.5 倍数，且 ≤ [3] 满分）
  [6] 点评      string  自然语言一段话；含"答出/问题/扣分/正确表述"要素；禁【】标签与内部评级词
```

**language_breakdown（六维度，每维 = `{score, reason}`）：**
```json
{
  "structure":    {"score": 1.5, "reason": "整体结构评语（≥12字，引用学生证据）"},  // 满分2
  "logic":        {"score": 1.5, "reason": "..."},  // 满分2
  "argument":     {"score": 1.0, "reason": "..."},  // 满分2
  "language":     {"score": 0.5, "reason": "..."},  // 满分1
  "history_view": {"score": 0.5, "reason": "..."},  // 满分1
  "neatness":     {"score": 1.5, "reason": "..."}   // 满分2（卷面整洁）
}
```
六维 `score` 之和须 = `language_score`（满分10）；`reason` 须为 ≥12 字自然语言评语，引用学生具体句子/术语。

**diagnosis（字符串，整体点评）：** 须同时含七要素——类卷（一二三类）、`X/40` 分数、要点踩分、组织论述、亮点、不足、提分空间；禁止 `属X类卷（...）` 括号内嵌理由写法。其中 `X/40` 必须等于 `points_score + language_score`。

**一致性硬约束（校验器拦截）：**
- 所有板块 `max_score` 之和 = 30；单板块 `max_score` ≤ 18
- 每板块 `score` = 其下 points 各 `[5]` 之和
- `points_score` = 各板块 `score` 之和；`language_score` = 六维 `score` 之和
- `points_score + language_score` ≤ 40，且 = `diagnosis` 中的 `X/40`
- 每踩分点 `[5]` 是 0.5 倍数且 ≤ `[3]`（满分）

---

## 生成前全局自检清单

**在调用 generate_word.py 生成 Word 之前，必须逐项完成以下自检，确认全部通过后方可生成。**

### A. 学生作答列自检
- [ ] 只摘录与踩分点直接相关的核心句，去除背景铺垫和过渡语句
- [ ] 无"学生答出""学生写了"等引导语
- [ ] 无双引号包裹
- [ ] 未涉及的踩分点 → 写"无"

### B. 整体点评自检
- [ ] 用完整句子表述判定依据，**禁止使用括号内嵌理由**
- [ ] 7个要素齐全：类卷等级及理由、得分预估、要点踩分情况、组织论述情况、亮点、不足、提分空间
- [ ] 得分预估与总分概览表一致
- [ ] 亮点/不足为概括性提及，不重复【本题诊断】中的具体内容

### C. 分数一致性自检
- [ ] 各板块内学生各点得分之和 = 板块得分
- [ ] 各板块满分之和 = 30
- [ ] 各板块踩分点分值之和 = 板块满分
- [ ] 六维度得分之和 = 论述组织（含卷面）得分
- [ ] 论述组织（含卷面）+ 要点踩点 = 总分概览合计
- [ ] 诊断文本中的分数 = 总分概览合计
- [ ] **参考答案每个子论点标注了分值**（格式：`（X分）`），子论点分值之和 = 板块满分
- [ ] **对照表"得分/满分"列同时显示学生得分和该要点满分**（格式：`3/4`）

### D. 参考答案自检
- [ ] 字数≥1000字
- [ ] 每个板块下的子论点分值之和 = 板块满分
- [ ] 子论点数量 = 踩分点数量
- [ ] 如已拆分子要点：每个踩分点的子要点分值之和 = 该踩分点分值
- [ ] 无AI腔禁用词（见AI用语规范表）
- [ ] 无英文残留
- [ ] 有开篇背景段和结尾升华段

### E. 点评格式自检
- [ ] 点评为自然语言一段话，不是填表格式
- [ ] 无【】标签输出给学生看
- [ ] 要素完整：答出了什么、哪里有问题、扣分、正确表述
- [ ] 流畅连贯，像老师口头点评

### F. 影响板块专项自检（仅适用于含"影响""意义""评价"的题目）
- [ ] 已查阅 `references/维度参考.md` 中对应历史分支的维度表
- [ ] 影响板块覆盖了政治、社会、对外关系等多个维度（不少于2个）
- [ ] 各维度独立成段（子论点），未混入同一维度

### G. 踩分点严格度自检（新增·2026-08-30）
- [ ] 踩分点核心关键词不超过5个
- [ ] 无战役战术细节（如"铁丝链连射手"等）
- [ ] 无政策全称要求（如《武家诸法度》应简化为"参勤交代制"）
- [ ] 无非标志年份（只保留1603年、1467年等标志性年份）
- [ ] 无制度目的说明作为独立踩分点

### H. 数值一致性自检（新增·2026-08-31）
- [ ] 总分概览表的"要点踩点"得分 = 各板块学生得分之和
- [ ] 总分概览表的"论述组织"得分 = 六维度得分之和
- [ ] 总分概览表的"合计" = 要点踩点 + 论述组织
- [ ] 诊断文本中的分数 = 总分概览表的合计
- [ ] **点评措辞与得分一致**：某踩分点点评含"给满分/满分/完整"等措辞时，该点得分率必须为100%；含"全扣/未答"时得分率必须为0%；不得出现"点评说给满分、得分却是50%"这类执行矛盾
- [ ] **板块标题唯一**：各板块标题（一）（二）（三）……不得重复，防止Word渲染时同一标题被重复输出
- [ ] **扣分区引用原文**：被扣分的踩分点，点评中必须引用学生答案原文中的关键表述（非自造），确保扣分有据可查

**自检不通过时，必须先修正数据再重新自检，禁止跳过。**

---

## 批量批改

当用户一次提供多道题目时，按以下规则处理：
1. **逐题独立批改**：每道题独立执行完整批改流程
2. **独立报告**：每道题生成独立 Word 报告
3. **统一目录**：所有报告保存在同一目录
4. **汇总表格**：全部批改完成后输出汇总表格

**注意**：批量批改不影响单题批改质量，每道题仍经过完整流程。

---

## 技术实现

### 多位一体约束模式

| 层级 | 文件 | 作用 | 对应检查项 |
|------|------|------|-----------|
| **规范层** | SKILL.md | 明确格式要求、示例、违规案例 | 本文件所有【检查点】 |
| **执行层** | generate_word.py | 结构化数据+自动校验+Word渲染 | 分数一致性校验、格式校验 |
| **验证层** | grade_checker.py | 独立自检，不通过则阻止生成 | A-F全局自检清单 |
| **参考层** | references/维度参考.md | 多维度分析参考工具 | F项维度覆盖检查 |
| **史实层** | historical_fact_checker.py | 史实错误数据库（可扩展） | 辅助第四步标红确认 |

**脚本调用顺序**：
```
第二步 generate_answer() → 第二步自检(D项)
第三步 extract_scoring_points() → 第三步自检(C项部分)
【阶段切换承诺】
第四步 ocr_student_answer() + red_marker() → 第四步自检(A项部分)
第五步 score_points() → 第五步自检(A项完整+E项+C项部分)
第六步 score_language() → 第六步自检
第七步 generate_diagnosis() → 第七步自检(B项完整)
全局自检（A-F）
grade_checker.py 验证
generate_word_report() 生成报告
```

> **数据约定（JSON字段，防卷面重复计入）**：`language_breakdown` 始终包含全部6个维度（含 `neatness`/卷面整洁）；渲染时顶层 `卷面` 字段必须置 `0`，因为 `set_grading_result` 已把 neatness 并入 `language` 的六维之和。严禁在 `language_breakdown` 之外再单独把卷面分加一遍，否则总分虚高2分（见 generate_report.py 修复记录）。

### 修改指南

本skill采用多位一体约束模式（规范层+执行层+验证层+参考层+史实层），修改时需同步更新以下文件，禁止只改一边。

> **回归校验（强制）**：凡修改本文件中的契约/公式/列宽/禁用词等规范条目，修改后必须重跑 `grade_checker.py` 全量校验（对 `grading_data_*.json` 逐份跑 `GradeChecker().run_all_checks`），确认 **8/8 全部 0 error** 再提交。这能挡住「规范↔代码漂移」类裂缝——例如 SKILL.md 写 `points+language`、代码却 `points+language+卷面` 这种同文件内 A 改 B 漏的情况（2026-09-01 审计即由此发现）。GradeChecker 只校验数据、不校验规范与代码是否一致，故此步需人工执行，不可省略。

#### 修改对应表

| 修改内容 | 需同步修改的文件 | 修改位置 |
|---------|----------------|---------|
| 新增/修改规范条目 | SKILL.md + grade_checker.py | 对应步骤的检查方法 |
| 新增输出格式要求 | SKILL.md + generate_word.py | 对应渲染逻辑 |
| 新增AI腔禁用词 | SKILL.md + ai_voice_detector.py（单一真源） | ai_voice_detector.py 的 AI_VOICE_PATTERNS / FORBIDDEN_WORDS |
| 新增史实错误库 | historical_fact_checker.py + SKILL.md示例 | HISTORY_ERRORS字典 |
| 新增维度表 | references/维度参考.md + grade_checker.py F项 | check_dimension_coverage() |
| 批量批改汇总格式 | SKILL.md + generate_word.py | 汇总表格渲染逻辑 |
| 参考答案英文残留检测 | generate_word.py + grade_checker.py | check_reference_answer() |
| 踩分点维度列校验 | grade_checker.py | check_scoring_points() |
| 踩分点严格度校验 | SKILL.md + grade_checker.py | check_scoring_detail_level()（新增） |
| 数值一致性校验 | SKILL.md + grade_checker.py + generate_word.py | check_score_sync()（新增） |
| 踩分点列格式校验 | SKILL.md + grade_checker.py | check_scoring_point_name()（新增） |
| 点评语言规范校验 | SKILL.md + grade_checker.py | check_comment_language()（新增） |
| 点评要素校验 | SKILL.md + grade_checker.py | check_comment_elements()（修改） |
| 重复踩分点校验 | SKILL.md + grade_checker.py | check_duplicate_points()（新增） |
| 论述组织评语格式 | SKILL.md + grade_checker.py | check_language_review_format()（新增·2026-08-31） |
| 反复合点校验 | SKILL.md + grade_checker.py | check_compound_points()（新增·2026-08-31，警告级） |
| 知识点维度组织（随题而选，允许缺失） | SKILL.md（第二步规则二）+ grade_checker.py | check_knowledge_dimension_coverage()（2026-08-31改为仅提示极端倾斜，不再强制全覆盖） |
| 参考答案独立三遍生成（多数合并取共识） | SKILL.md（第二步）+ generate_answer.py + grade_checker.py | generate_answer.py 提供 merge_passes()；grade_checker.py 新增 check_three_passes() 校验 three_passes 字段 |
| 得分精度必须为0.5倍数 | SKILL.md（第五步）+ grade_checker.py | 学生得分必须为0.5步长，非0.5倍数报错（新增·2026-08-31） |
| 输出自检防bug（点评≡得分/标题唯一/扣分引用原文） | SKILL.md（全局自检H项） | H项三条硬检查（新增·2026-08-31） |
| 相对路径约束（禁止写死绝对路径） | SKILL.md（第二步） | 全部文件引用改相对路径（新增·2026-08-31） |

#### 修改顺序（必须遵守）

1. **先在SKILL.md中写明新规范**（含示例、违规案例、检查点）
2. **再在grade_checker.py中实现对应检查**（新增或修改check_*方法）
3. **最后在generate_word.py中实现对应输出**（修改渲染逻辑）
4. **运行grade_checker.py测试验证**（使用test_grading.json或真实数据）
5. **检查无error后再提交**

**为什么必须按此顺序？** grade_checker.py依赖SKILL.md的规范定义，generate_word.py依赖两者的输出格式。顺序颠倒会导致规范与实现不一致。

> **多位一体一致性验证（强制，改完必做）**：跨文件改动（涉及 SKILL.md、grade_checker.py、generate_word.py 中任两个以上）完成后，**禁止直接交付**。必须完成以下两步验证，全部通过才算改动完成：
> 1. **跑通验证**：用真实批改数据跑一次 `grade_checker.py`，确认 0 ERROR；
> 2. **逐条核对**：对照上方"修改对应表"，把本次改动涉及的每一行逐一确认——SKILL.md 的规范描述、grade_checker.py 的 `check_*` 函数、generate_word.py 的渲染逻辑三者是否一一对应、命名是否与对应表一致。
>
> 二者缺一即视为"只改一边"，判定为未完成，退回补改。**本 skill 没有专门的一致性比对脚本，验证靠"跑 grade_checker + 人工核对对应表"两步完成**（轻量方案，不加重型工具）。

#### 违反此流程的后果

规范写了但执行不到 → AI继续犯同样的错误 → 和本次重构前的问题一样。
